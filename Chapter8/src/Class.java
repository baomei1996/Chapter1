public class Class {
    // 상속이란 클래스를 정의 할 때 이미 구현된 클래스를 상속(Inheritance) 받아서
    // 속성이나 기능이 확장되는 클래스를 구현함

    // 상속하는 클래스 : 상위클래스, parent class, base class, super class
    // 상속받는 클래스 : 하위클래스, child class, derived class, subclass

    // 클래스 상소 문법 class B extends A {}
}
